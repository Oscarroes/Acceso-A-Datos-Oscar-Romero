#include <stdio.h>

int main (int argc,char *argv[]){
    printf("suma: %i \n", (4+3)); //el %i indica que lo que hay son enteros
    printf("resta: %i \n", (4-3));
    printf("multiplicacion: %i \n", (4*3));
    printf("division: %f \n", (16/3));//la f indica que es float
    printf("resto entero de la division: %i \n", (4%3));
    
    return 0;
}